# Non-secret Docker Compose defaults for the DGX deployment.
SPARK_AUDIUS_ENABLED=false

# The deploy script replaces this with a content-hash image tag.
SPARK_DEMO_IMAGE=spark-active-companion-demo:local
